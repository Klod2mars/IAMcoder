mission:
  name: Command Tree Simple
  description: Explore la structure du projet et génère un fichier Markdown listant les dossiers et fichiers principaux.

  tasks:
    - name: Génération du Command Tree
      type: bash
      params:
        command: |
          echo "# 🌳 Structure du Projet" > structure_simple.md
          echo "" >> structure_simple.md
          echo "\`\`\`" >> structure_simple.md
          tree -L 3 --dirsfirst >> structure_simple.md
          echo "\`\`\`" >> structure_simple.md

    - name: Vérification du fichier généré
      type: read
      params:
        file: "structure_simple.md"
