# cursor_audit_goal.yaml
name: "Audit_TaskGoal_Field"
mode: read_only
tasks:
  - name: "analyze_task_goal"
    task_type: "analysis"
    goal: "Déterminer le rôle réel du champ 'goal' dans AIHomeCoder"
    parameters:
      plan:
        target_scope:
          - "domain/entities/task.py"
          - "data/yaml_parser.py"
          - "domain/services/task_logic_handler.py"
          - "core/context_bridge.py"
        questions:
          - "Le champ 'goal' est-il référencé en dehors de la validation initiale de la classe Task ?"
          - "Est-il utilisé dans le logging, les rapports Markdown, le context_bridge ou les diagnostics ?"
          - "Y a-t-il des dépendances implicites (ex. context_meta, mission.metadata, etc.) liées à ce champ ?"
          - "En V2, peut-on le rendre optionnel sans casser la compatibilité rétroactive ?"
        report:
          destination: "reports/audit_goal_field.md"
